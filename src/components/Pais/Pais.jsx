import React from 'react'

const Pais = (min, max, img, id, wind, temp, name, weather, clouds, latitud, longitud) => {
    return (
        <div>
            <h2>{name}</h2>
        </div>
    )
}

export default Pais
